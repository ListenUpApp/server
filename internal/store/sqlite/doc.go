// Package sqlite implements the listenup-server Store interface against a
// modernc/sqlite database. Each domain entity (books, contributors, series,
// libraries, listening sessions, ...) lives in its own file within the package.
package sqlite
